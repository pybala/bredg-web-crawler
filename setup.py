from setuptools import setup, find_packages


setup(
    name="bredg-web-crawl",
    version="1.0",
    packages=find_packages(),
	install_requires=[],
    entry_points={},
    tests_require=['pytest'],
)